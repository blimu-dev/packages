---
"@blimu/codegen": minor
---

Add `srcDir` configuration option to customize the source directory path for generated SDK output. This allows placing generated code in subdirectories like `src/sdk` instead of the default `src`.
